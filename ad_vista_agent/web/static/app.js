const state={run:null,session:null,file:null,deliverables:[],busy:false,operation:0,activeJob:null};
const $=(selector,root=document)=>root.querySelector(selector);
const $$=(selector,root=document)=>[...root.querySelectorAll(selector)];
const esc=value=>String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[char]));
const visibleText=value=>String(value??"").replace(/\b(?:speech|ocr_cluster|kf)_[0-9a-z_]+\b/gi,"").replace(/[ \t]{2,}/g," ");
async function api(url,options){const response=await fetch(url,options);const text=await response.text();let value={};try{value=text?JSON.parse(text):{}}catch{value={error:text}}if(!response.ok)throw Error(value.error||`请求失败（${response.status}）`);return value}
const formatTime=ms=>{const value=Math.max(0,Number(ms)||0)/1000;return `${String(Math.floor(value/60)).padStart(2,"0")}:${(value%60).toFixed(1).padStart(4,"0")}`};
function renderCitations(row,citations=[]){if(!citations.length)return;const box=document.createElement("div");box.className="citations";box.innerHTML=citations.map(item=>`<article class="citation ${esc(item.type||"")}">${item.thumbnail_url?`<img src="${esc(item.thumbnail_url)}" alt="对应关键画面">`:""}<div><strong>${esc(item.label||"证据")}</strong>${item.start_ms!==undefined?`<time>${formatTime(item.start_ms)}</time>`:""}<p>${esc(item.excerpt||"该证据没有可显示摘要")}</p></div></article>`).join("");row.appendChild(box)}
function addMessage(role,text,citations=[],actions=[]){$("#welcome").classList.add("hidden");const row=document.createElement("div");row.className=`message-row ${role}`;row.innerHTML=`<div class="avatar">A</div><div class="bubble">${esc(visibleText(text))}</div>`;renderCitations(row,citations);if(actions.length){const links=document.createElement("div");links.className="result-actions";for(const action of actions){const link=document.createElement("a");link.className="result-action";link.href=action.href;link.target="_blank";link.rel="noopener";link.textContent=action.label;links.appendChild(link)}row.appendChild(links)}$("#messages").appendChild(row);$("#conversation").scrollTop=$("#conversation").scrollHeight;return row}
function setBusy(value){state.busy=value;$("#send").disabled=value;$("#prompt").disabled=value;$("#send").textContent=value?"…":"↑"}
function closeComposerMenu(){$("#composer-menu").classList.add("hidden");$("#composer-plus").setAttribute("aria-expanded","false")}
const deliverableLabels={evidence:"证据提取",insights:"卖点分析",risk_audit:"风险复核",report:"卖点分析报告",creative:"创意建议"};
function updateComposerMode(){const summary=$("#deliverable-summary");if(state.deliverables.length){summary.textContent=state.deliverables.map(item=>deliverableLabels[item]).join(" · ");summary.classList.remove("hidden")}else{summary.classList.add("hidden");summary.textContent=""}$("#mode-hint").textContent=state.file?(state.deliverables.length?"按所选任务生成":"AdInsight-RL 自动识别卖点"):state.run?"继续询问当前视频":"普通对话"}
function showJob(text,jobId){const box=$("#job");box.classList.remove("hidden");box.innerHTML=`<span>${esc(text)}</span><div><button type="button" id="background-job">后台运行</button><button type="button" id="cancel-job">取消任务</button></div>`;$("#background-job").onclick=()=>{state.operation++;state.activeJob=null;box.classList.add("hidden");setBusy(false);addMessage("assistant","任务将在后台继续运行，完成后可从左侧任务列表打开。")};$("#cancel-job").onclick=async()=>{state.operation++;state.activeJob=null;try{await api(`/api/jobs/${jobId}`,{method:"DELETE"});addMessage("assistant","已请求取消任务；当前步骤结束后会停止。")}catch(error){addMessage("assistant",`取消失败：${error.message}`)}finally{box.classList.add("hidden");setBusy(false)}}}
function showConfirmation(runId,agent){const box=$("#job");box.classList.remove("hidden");const reason=agent?.reflections?.at(-1)?.reason||"风险复核需要人工确认";box.innerHTML=`<span>待确认：${esc(reason)}</span><div><button type="button" id="approve-job">确认继续</button><button type="button" id="reject-job">拒绝并停止</button></div>`;$("#approve-job").onclick=async()=>{try{await api(`/api/runs/${runId}/confirmation`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({decision:"approve"})});box.classList.add("hidden");await bindRun(runId,true)}catch(error){addMessage("assistant",`确认失败：${error.message}`)}};$("#reject-job").onclick=async()=>{try{await api(`/api/runs/${runId}/confirmation`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({decision:"reject",reason:"用户拒绝人工确认"})});box.classList.add("hidden");await loadRuns()}catch(error){addMessage("assistant",`拒绝失败：${error.message}`)}}}
function resultActions(runId,deliverables){const actions=[];const selected=new Set(deliverables||[]);if(selected.has("report")){actions.push({label:"下载卖点分析报告（HTML）",href:`/api/runs/${runId}/exports/report-html`},{label:"下载卖点分析报告（Markdown）",href:`/api/runs/${runId}/exports/report-markdown`})}if(selected.has("insights")){actions.push({label:"下载卖点洞察 JSON",href:`/api/runs/${runId}/exports/analysis-json`})}if(selected.has("evidence")){actions.push({label:"下载证据账本 JSON",href:`/api/runs/${runId}/exports/evidence-json`})}if(selected.has("risk_audit")){actions.push({label:"下载风险复核 JSON",href:`/api/runs/${runId}/exports/audit-json`})}if(selected.has("creative")){actions.push({label:"下载创意建议 JSON",href:`/api/runs/${runId}/exports/creative-json`})}return actions}
async function uploadVideo(text){const token=++state.operation;const goal=text||"请识别这个广告视频的核心卖点";const selected=state.deliverables.slice();const form=new FormData();form.append("video",state.file);form.append("goal",goal);form.append("mode",/(深度|全面|详细|完整|deep)/i.test(goal)?"deep":"quick");form.append("deliverables",JSON.stringify(selected));const job=await api("/api/uploads",{method:"POST",body:form});if(token!==state.operation)return;state.activeJob=job.job_id;showJob("任务排队中…",job.job_id);const labels={ingest:"读取视频",timeline:"分析镜头并提取关键帧",speech:"识别语音",ocr:"识别画面文字",ledger:"整理证据",insights:"识别广告卖点",report:"生成卖点分析报告",creative:"生成创意建议"};for(;;){const value=await api(`/api/jobs/${job.job_id}`);if(token!==state.operation)return;if(value.status==="failed"){$("#job").classList.add("hidden");state.activeJob=null;throw Error(value.error)}if(value.status==="cancelled"){$("#job").classList.add("hidden");state.activeJob=null;addMessage("assistant","任务已取消。");return}const progress=value.current_tool?`正在${labels[value.current_tool]||value.current_tool}…`:value.status==="running"?"正在准备视频分析…":value.status==="cancelling"?"正在当前步骤结束后取消…":"任务排队中…";showJob(progress,job.job_id);if(value.status==="completed"||value.status==="waiting_confirmation"){$("#job").classList.add("hidden");state.activeJob=null;state.file=null;state.deliverables=[];$("#attachment").classList.add("hidden");$("#video").value="";$$('.deliverable-option input').forEach(input=>input.checked=false);updateComposerMode();const runId=value.execution_id||value.run_id;await bindRun(runId,false);const generated=Object.keys(value.result?.deliverables||{});const actions=resultActions(runId,generated.length?generated:selected);addMessage("assistant",value.status==="waiting_confirmation"?"卖点分析已生成，但风险复核需要人工确认。下载链接已附在当前消息中。":"分析已完成。下载链接已附在当前消息中。",[],actions);return}await new Promise(resolve=>setTimeout(resolve,1200))}}
async function readAnswerStream(url,body){const response=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});if(!response.ok)throw Error(`请求失败（${response.status}）`);const row=addMessage("assistant","");const bubble=$(".bubble",row);const reader=response.body.getReader();const decoder=new TextDecoder();let pending="",answer="",citations=[],reportUrl=null,terminal=false;for(;;){const {value,done}=await reader.read();pending+=decoder.decode(value||new Uint8Array(),{stream:!done});const lines=pending.split("\n");pending=lines.pop()||"";for(const line of lines){if(!line)continue;const event=JSON.parse(line);if(event.type==="status"&&!answer)bubble.textContent=event.text;if(event.type==="delta"){answer+=event.text;bubble.textContent=visibleText(answer)}if(event.type==="error"){terminal=true;throw Error(event.message||"流式响应失败")}if(event.type==="done"){terminal=true;reportUrl=event.report_url||null;if(event.message?.content)answer=event.message.content;citations=event.message?.citations||[]}}if(done)break}if(!terminal)throw Error("流式响应意外中断");bubble.textContent=visibleText(answer);renderCitations(row,citations);if(reportUrl){const links=document.createElement("div");links.className="result-actions";const link=document.createElement("a");link.className="result-action";link.href=reportUrl;link.target="_blank";link.rel="noopener";link.textContent="下载卖点分析报告（HTML）";links.appendChild(link);row.appendChild(links)}}
async function sendMessage(text){if(state.run)await readAnswerStream(`/api/runs/${state.run}/messages/stream`,{question:text});else await readAnswerStream("/api/chat/stream",{message:text,session_id:state.session})}
function clearConversation(){state.operation++;state.activeJob=null;state.run=null;state.session=null;state.file=null;state.deliverables=[];$("#job").classList.add("hidden");$("#messages").innerHTML="";$("#welcome").classList.remove("hidden");$("#attachment").classList.add("hidden");$("#video").value="";$$('.deliverable-option input').forEach(input=>input.checked=false);updateComposerMode();closeComposerMenu();$("#header-title").textContent="AdVista";$("#header-subtitle").textContent="广告卖点分析助手";$("#sidebar").classList.remove("open");setBusy(false);loadRuns()}
async function loadRuns(){try{const data=await api("/api/runs");const runs=data.runs||[];$("#run-count").textContent=runs.length;const box=$("#runs");box.innerHTML=runs.map(run=>`<button class="run ${run.run_id===state.run?"selected":""}" data-run="${esc(run.run_id)}"><span class="run-title">${esc(run.run_id)}</span><span class="run-meta"><span>${Object.keys(run.artifacts||{}).length} 个结果</span><span>${esc(run.status_label||run.status||"已保存")}</span></span></button>`).join("");$$('.run',box).forEach(button=>button.onclick=()=>bindRun(button.dataset.run,true))}catch(error){$("#boot-error").textContent=`任务列表加载失败：${error.message}`;$("#boot-error").classList.remove("hidden")}}
async function bindRun(runId,loadMessages=true){const token=++state.operation;state.activeJob=null;$("#job").classList.add("hidden");let data={messages:[]},detail={};try{if(loadMessages)[data,detail]=await Promise.all([api(`/api/runs/${runId}/messages`),api(`/api/runs/${runId}`)]);else detail=await api(`/api/runs/${runId}`)}catch(error){addMessage("assistant",`任务状态加载失败：${error.message}`);return false}if(token!==state.operation)return false;state.run=runId;state.session=null;$("#header-title").textContent="视频卖点分析";$("#header-subtitle").textContent=runId;$("#mode-hint").textContent="继续询问当前视频";$("#sidebar").classList.remove("open");if(loadMessages){$("#messages").innerHTML="";$("#welcome").classList.add("hidden");if(data.messages?.length)data.messages.forEach(message=>addMessage(message.role,message.content,message.citations||[]));else addMessage("assistant","已切换到这个视频任务。你可以继续询问广告卖点、受众、字幕或画面内容。")}if(detail.agent?.status==="waiting_confirmation")showConfirmation(runId,detail.agent);await loadRuns();return true}
$("#prompt-form").onsubmit=async event=>{event.preventDefault();if(state.busy)return;const input=$("#prompt"),text=input.value.trim();if(!text&&!state.file)return;input.value="";if(text)addMessage("user",text);else addMessage("user",`已附加视频：${state.file.name}`);setBusy(true);try{if(state.file)await uploadVideo(text);else await sendMessage(text)}catch(error){addMessage("assistant",`请求失败：${error.message}`)}finally{setBusy(false)}};
$("#prompt").addEventListener("keydown",event=>{if(event.isComposing||event.keyCode===229)return;if(event.key==="Enter"&&!event.shiftKey){event.preventDefault();$("#prompt-form").requestSubmit()}});
$("#composer-plus").onclick=event=>{event.stopPropagation();const menu=$("#composer-menu"),opening=menu.classList.contains("hidden");menu.classList.toggle("hidden",!opening);$("#composer-plus").setAttribute("aria-expanded",String(opening))};$("#composer-menu").onclick=event=>event.stopPropagation();document.addEventListener("click",closeComposerMenu);
$$('.deliverable-option input').forEach(input=>input.onchange=()=>{state.deliverables=$$('.deliverable-option input:checked').map(item=>item.value);updateComposerMode()});$("#video").onchange=event=>{state.file=event.target.files[0]||null;if(state.file){$("#attachment-name").textContent=state.file.name;$("#attachment").classList.remove("hidden");closeComposerMenu();updateComposerMode()}};$("#remove-attachment").onclick=()=>{state.file=null;$("#video").value="";$("#attachment").classList.add("hidden");updateComposerMode()};$("#new-chat").onclick=clearConversation;$("#refresh").onclick=loadRuns;$("#menu").onclick=()=>$("#sidebar").classList.toggle("open");
window.addEventListener("error",event=>{const box=$("#boot-error");if(box){box.textContent=`页面脚本错误：${event.message}`;box.classList.remove("hidden")}});async function init(){try{await api("/api/health");$("#health").textContent="服务在线"}catch{$("#health").textContent="服务离线"}await loadRuns()}init().catch(error=>{$("#boot-error").textContent=`页面初始化失败：${error.message}`;$("#boot-error").classList.remove("hidden")});
